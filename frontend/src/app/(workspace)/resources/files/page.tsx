import { ResourcesView } from "@/components/pages/resources/ResourcesView";

/** 文件库（默认 Tab）：上传 / 文件夹 / 会话附件。 */
export default function ResourcesFilesPage() {
  return <ResourcesView tab="files" />;
}
