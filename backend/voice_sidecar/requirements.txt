# Direct/runtime dependencies for the local Chinese MeloTTS sidecar.
# MeloTTS is mounted from backend/vendor/MeloTTS at a pinned revision instead
# of installed as a package, so its setup.py post-install hook is not run.
# That avoids downloading the optional Japanese unidic dictionary.
#
# melo/text/cleaner.py imports all language modules at module import time.
# melo_bootstrap.py supplies fail-loud stubs for the unused Japanese/Korean
# imports, so this file contains only dependencies used by the sidecar and
# the active ZH/ZH_MIX_EN import/inference path.  Do not infer a complete
# license list from this file alone: audit the resolved wheel metadata/SBOM
# whenever distributing a virtualenv or image.

# Sidecar HTTP service
fastapi
uvicorn
pydantic

# MeloTTS inference and model loading
# torch/torchaudio are installed first from the CPU-only PyTorch index by
# deploy/install_voice.sh.
torch
torchaudio
transformers==4.27.4
numpy==1.26.4
scipy
soundfile
librosa
cached-path
huggingface_hub
tqdm

# Chinese and mixed Chinese/English text processing
jieba
pypinyin
cn2an
g2p_en
nltk
